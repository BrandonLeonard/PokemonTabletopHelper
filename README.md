# PokemonTabletopHelper

run `python3` in command line

in the interpreter run:
```
import PokemonTabletopHelper as pth
trainer = pth.Trainer("Trainer")
pth.createStandardPokes(trainer)
gengar = trainer.pokemon['Gengar']

```
Have fun.
